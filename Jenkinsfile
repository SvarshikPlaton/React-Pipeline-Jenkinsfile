pipeline {
    agent { label 'React' }

    stages {
        stage('clone') {
            steps {
                
            }
        }

        stage("build") {
            steps {
                
            }
        }

        stage("deploy") {
            steps {
                
            }
        }
    }   
}
